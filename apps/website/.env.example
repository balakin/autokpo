# PostHog analytics (optional — analytics no-ops when either is unset).
# PUBLIC_* vars are exposed to the client and inlined into the bundle at build time.
PUBLIC_POSTHOG_PROJECT_TOKEN=phc_xxxxxxxxxxxxxxxxxxxxxxxx
PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com
